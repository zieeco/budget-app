<section class="vh-100 d-flex flex-column">
  <div class="d-head d-flex align-items-center justify-content-between pt-3 pb-3 ps-e-12">
    <%= render "/shared/back" %>
    <h2 class="no-mb">LOGIN</h2>
    <span>Next</span>
  </div>
  <div class="bg-body">
    <div class="login-user d-flex flex-column pb-5 col-md-8 col-lg-6 container">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), :html => {:class => "form-style bg-white container-fluid pt-cent"}) do |f| %>

        <div class="form-bdy">

          <div class="mb-3">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control border-top-0 border-end-0 border-start-0", placeholder: "Email address" %>
          </div>

          <div class="">
            <%= f.password_field :password, autocomplete: "current-password",  autofocus: true, class: "form-control border-top-0 border-end-0 border-start-0", placeholder: "Password" %>
          </div>

          <div class="text text-end mb-3 fs-sm-1">
            <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
              <%= link_to "Forgot password?", new_password_path(resource_name) %><br />
            <% end %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="mb-2 fs-sm-1">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          <% end %>

          <div class="text fs-sm-1 text text-center mb-4">
            <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
              Need to create an account?
              <%= link_to "Sign Up", new_registration_path(resource_name) %><br />
            <% end %>
          </div>

          <div>
            <%= f.submit value: 'Submit', class: 'btn btn-primary' %>
          </div>

        </div>
      <% end %>
    </div>
  </div>
</section>
